# Python Backend Requirements for ONBOARD F1 Dashboard
# FastAPI backend for live timing and race data
# Compatible with Python 3.8+

fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# SignalR for F1 Live Timing
signalrcore==0.9.5

# Optional: Redis for caching (comment out if not using)
# redis==5.2.0

# FastF1 for historical race data (optional, requires Python 3.9+)
# fastf1==3.4.0

# Data processing - compatible versions
pandas>=2.0.0
numpy>=1.24.0

# Additional dependencies
python-multipart==0.0.12
aiofiles==24.1.0
websockets==13.1

# Auth dependencies (optional)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
